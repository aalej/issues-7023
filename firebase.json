{
  "firestore": [
    {
      "database": "default",
      "rules": "firestore.default.rules",
      "indexes": "firestore.default.indexes.json"
    },
    {
      "database": "test-db",
      "rules": "firestore.test-db.rules",
      "indexes": "firestore.test-db.indexes.json"
    }
  ]
}